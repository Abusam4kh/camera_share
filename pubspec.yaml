name: camera_share
description: مشاركة الكاميرا عبر شبكة Wi-Fi محلية بدون إنترنت.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.12.1
  sqflite: ^2.4.4
  path: ^1.9.1
  path_provider: ^2.1.6
  permission_handler: ^13.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
